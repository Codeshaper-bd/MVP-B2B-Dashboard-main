import PromoterLayout from "@/components/partials/layouts/PromoterLayout";

function Layout({ children }: { children: React.ReactNode }) {
  return <PromoterLayout>{children}</PromoterLayout>;
}

export default Layout;
